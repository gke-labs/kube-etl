apiVersion: syncer.gkelabs.io/v1alpha1
kind: KRMSyncer
metadata:
  name: suspend-syncer
  namespace: default
spec:
  suspend: true
  syncMode: push
  remote:
    clusterConfig:
      kubeConfigSecretRef:
        name: dest-kubeconfig
  rules:
  - group: e2e.gkelabs.io
    version: v1alpha1
    kind: TestCRD
