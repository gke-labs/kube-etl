---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: krmsyncers.syncer.gkelabs.io
spec:
  group: syncer.gkelabs.io
  names:
    kind: KRMSyncer
    listKind: KRMSyncerList
    plural: krmsyncers
    singular: krmsyncer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KRMSyncer is the Schema for the krmsyncers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KRMSyncerSpec defines the desired state.
            properties:
              destination:
                description: Destination defines the target for the sync.
                properties:
                  clusterConfig:
                    description: ClusterConfig defines the configuration for syncing
                      to a remote Kubernetes cluster.
                    properties:
                      kubeConfigSecretRef:
                        description: |-
                          KubeConfigSecretRef is the reference to the secret containing the
                          kubeconfig of the destination cluster.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - kubeConfigSecretRef
                    type: object
                type: object
              rules:
                description: Rules defines which resources to watch and sync. If unset,
                  sync all resources by default.
                items:
                  description: ResourceRule defines criteria for what to sync.
                  properties:
                    group:
                      description: Group is the API group of the resource to be synchronized.
                      type: string
                    kind:
                      description: Kind is the Kind of the resource to be synchronized.
                      type: string
                    namespaces:
                      description: Namespaces is an optional list of namespaces to
                        watch. If not provided, all namespaces are synchronized.
                      items:
                        type: string
                      type: array
                    transforms:
                      description: Transforms defines a list of transformations to
                        apply to the resource before syncing.
                      items:
                        description: Transformation defines a modification to be applied
                          to a resource.
                        properties:
                          fieldTransform:
                            description: |-
                              FieldTransform allows mapping a value from a source field (e.g., status)
                              to a destination field (e.g., spec) before syncing.
                            properties:
                              destination:
                                description: Destination is the JSON path to the field
                                  to set the value to (e.g., "spec.resourceID").
                                type: string
                              source:
                                description: Source is the JSON path to the field
                                  to copy the value from (e.g., "status.externalRef").
                                type: string
                            required:
                            - destination
                            - source
                            type: object
                        type: object
                      type: array
                    version:
                      description: Version is the API version of the resource to be
                        synchronized.
                      type: string
                  required:
                  - group
                  - kind
                  - version
                  type: object
                type: array
              suspend:
                description: Suspend tells the controller to suspend the sync operations.
                type: boolean
            required:
            - destination
            - rules
            type: object
          status:
            description: KRMSyncerStatus defines the observed state.
            properties:
              conditions:
                description: Conditions of the Syncer.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
